import type { AnimationTemplateId } from '@/engine/scene/types'

/**
 * Reserved for v1.1. No template modules live here yet — they'll be added
 * alongside looping playback support.
 */
export const AMBIENT_TEMPLATE_IDS: AnimationTemplateId[] = [
  'breathe',
  'float',
  'drift',
  'shimmer',
  'none',
]
