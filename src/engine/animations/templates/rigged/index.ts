import type { AnimationTemplateId } from '@/engine/scene/types'

/**
 * Reserved for v1.2. No template modules live here yet — they'll be added
 * alongside the multi-phase keyframe engine.
 */
export const RIGGED_TEMPLATE_IDS: AnimationTemplateId[] = [
  'walk-cycle',
  'wave',
  'idle-sway',
  'none',
]
